import "core-js"

import {validate} from "./validator";
import {
  Required,
  MinLength,
  MaxLength,
  Regex,
  Email,
} from "./validators";

export {
  Required,
  MinLength,
  MaxLength,
  Regex,
  Email,
  validate
}
